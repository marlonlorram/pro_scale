name: pro_scale_example
description: "Demonstrates pro_scale: static (100.w) and reactive (context.scale) scaling."
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  pro_scale:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
