name: pro_scale
description: "Screen-adaptive scaling for size, spacing, radius and fonts, with automatic orientation swap and accessibility-aware font sizing."
version: 0.0.1
homepage: https://github.com/marlonlorram/pro_scale
repository: https://github.com/marlonlorram/pro_scale
issue_tracker: https://github.com/marlonlorram/pro_scale/issues

topics:
  - responsive
  - scaling
  - adaptive
  - layout
  - accessibility

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
